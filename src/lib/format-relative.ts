/**
 * Re-exports from the centralized date formatting utility.
 * All date formatting in the product uses `src/lib/format-date.ts`.
 */
export { formatRelative, formatDate } from '@/lib/format-date';
