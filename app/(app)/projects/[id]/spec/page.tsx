import { StageStub } from '@/components/forge/StageStub';

/** Spec stage stub (real content arrives in Spec 4). */
export default function SpecStubPage() {
  return <StageStub title="Spec" comingIn="Spec 4" />;
}
